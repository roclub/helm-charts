apiVersion: v2
name: kubernetes-bootstrap
description: This Helm chart is used to bootstrap Kubernetes clusters the roclub way.
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
- name: agent
  version: 1.6.8
  repository: https://charts.sysdig.com
  condition: sysdigSecure.enabled