{{- if .Values.awsEBSDriver.enabled -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: '{{ .Values.applicationPrefix }}-aws-ebs-driver'
  namespace: argocd
spec:
    project: {{ .Values.project }}
    source:
        repoURL: https://kubernetes-sigs.github.io/aws-ebs-csi-driver
        targetRevision: {{ .Values.awsEBSDriver.targetRevision }}  
        chart: aws-ebs-csi-driver
        helm:
            releaseName: '{{ .Values.releaseNamePrefix }}-aws-ebs-csi-driver'
            values: |
              controller:
                loggingFormat: json
                priorityClassName: csi-drivers

                serviceAccount:
                  annotations:
                    eks.amazonaws.com/role-arn: {{ .Values.awsEBSDriver.awsRoleArn }}
              node:
                loggingFormat: json
                priorityClassName: csi-drivers

              storageClasses:
              - name: gp3
                volumeBindingMode: WaitForFirstConsumer
                reclaimPolicy: Retain
                parameters:
                  encrypted: "true"
                  type: gp3
              - name: aws-gp3-daily-backup
                volumeBindingMode: WaitForFirstConsumer
                reclaimPolicy: Retain
                parameters:
                  encrypted: "true"
                  type: gp3
                  tagSpecification_1: "backup=true"
                  tagSpecification_2: "frequency=daily"
              - name: aws-gp3-weekly-backup
                volumeBindingMode: WaitForFirstConsumer
                reclaimPolicy: Retain
                parameters:
                  encrypted: "true"
                  type: gp3
                  tagSpecification_1: "backup=true"
                  tagSpecification_2: "frequency=weekly"
              - name: aws-gp3-monthly-backup
                volumeBindingMode: WaitForFirstConsumer
                reclaimPolicy: Retain
                parameters:
                  encrypted: "true"
                  type: gp3
                  tagSpecification_1: "backup=true"
                  tagSpecification_2: "frequency=monthly"

    destination:
        name: {{ .Values.cluster }}
        namespace: {{ .Values.awsEBSDriver.namespace }}

    syncPolicy:
        automated:
            prune: true
            selfHeal: true
            allowEmpty: false

        syncOptions:
            - Validate=true
            - CreateNamespace=true
            - PrunePropagationPolicy=foreground
            - PruneLast=true 

        retry:
            limit: 3
            backoff:
                duration: 10s
                factor: 2
                maxDuration: 3m
{{- end }}  