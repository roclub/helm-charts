apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ include "controlplane-app.fullname" . }}-config"
  labels:
    {{- include "controlplane-app.selectorLabelsApp" . | nindent 4 }}
data:
  POSTGRES_URL: {{ .Values.app.config.postgresUrl | quote }}
  LOG_LEVEL: {{ .Values.app.config.logLevel | quote }}
  ENV: {{ .Values.app.config.env | quote }}
