{{- if .Values.authority.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "roclub-app-backend-connector-registration.authorityName" . }}
  labels:
    {{- include "roclub-app-backend-connector-registration.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "roclub-app-backend-connector-registration.authorityName" . }}
  minReplicas: {{ .Values.authority.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.authority.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.authority.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.authority.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.authority.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.authority.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
