apiVersion: v1
kind: Service
metadata:
  name: {{ include "roclub-app-backend-connector-registration.gatewayName" . }}
  labels:
    {{- include "roclub-app-backend-connector-registration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.gateway.service.type }}
  ports:
    - port: {{ .Values.gateway.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "roclub-app-backend-connector-registration.selectorLabels" . | nindent 4 }}