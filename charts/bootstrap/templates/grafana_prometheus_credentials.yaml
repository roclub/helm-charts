{{- if .Values.prometheus.monitorInstance.enabled }}
{{ $namespace := .Values.prometheus.monitorInstance.namespace }}
{{- range .Values.prometheus.monitorInstance.remoteWrite }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .name }}
  namespace: {{ $namespace }}
stringData:
  username: "{{ .basicAuth.username }}"
  password: "{{ .basicAuth.password }}"
{{- end }}
{{- end }}
